# Test environment overrides — loaded when APP_ENV=test
# Safe to commit: uses only fake/placeholder values.

AGENT_SHARED_SECRET=test-secret-xyz
OPENAI_API_KEY=sk-test-placeholder
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

HOST=0.0.0.0
PORT=8081
LOG_LEVEL=DEBUG

AGENT_MAX_ITERATIONS=4
AGENT_REQUEST_TIMEOUT_S=10
